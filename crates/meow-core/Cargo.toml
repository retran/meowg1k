[package]
name = "meow-core"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
# Internal to the workspace. Only the `meow` binary ships, so these carry no
# version contract and cargo-deny can treat their path dependencies as the
# non-hazard they are.
publish = false
description = "Domain types shared across the meowg1k workspace"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }

[lints]
workspace = true
