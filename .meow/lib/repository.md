This is meowg1k, a script-friendly AI companion. Users write their commands in
Starlark; the Rust binary supplies the runtime, the model gateways, the session
store, and the terminal.

`docs/spec/` is normative and every behaviour traces to a requirement there. A
change that no requirement describes is a change that needs a requirement
first, not a change that needs shipping.
