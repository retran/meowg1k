version: "2"

run:
  go: "1.25.2"
  timeout: 3m
  issues-exit-code: 1

linters:
  enable:
    - govet
    - staticcheck
    - unused
    - ineffassign
    - bodyclose
    - misspell
  disable:
    - errcheck

formatters:
  enable:
    - gofumpt
    - gci

  settings:
    gofumpt:
      extra-rules: true

    gci:
      sections:
        - standard
        - default
        - prefix(github.com/retran/meowg1k)
      custom-order: true

issues:
  max-issues-per-linter: 0
  max-same-issues: 0